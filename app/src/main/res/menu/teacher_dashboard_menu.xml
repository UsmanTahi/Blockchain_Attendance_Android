<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/teacher_logout"
        android:title="Logout"
        app:showAsAction="never"/>

    <item android:id="@+id/add_batch"
        android:icon="@drawable/batch_add_ic"
        android:title="add batch"
        app:showAsAction="always"/>

    <item android:id="@+id/connectEth"
        android:title="Connect to Blockchain"
        app:showAsAction="never"/>

    <item android:id="@+id/checkContract"
        android:title="Check/Deploy Contract"
        app:showAsAction="never"/>

    <item android:id="@+id/generateAllSList"
        android:title="generate list of students"
        app:showAsAction="never"/>

    <item android:id="@+id/checkWallet"
        android:title="Check Wallet"
        app:showAsAction="never"/>

</menu>